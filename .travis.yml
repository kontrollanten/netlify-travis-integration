os:
  - linux

dist: trusty

language: node_js
node_js: 6

cache:
  directories:
    - node_modules

install:
  - npm install yarn -g
  - yarn

jobs:
  include:
    - stage: lint
      script: yarn lint
    - stage: test
      script: yarn test
